<style>
  .mr-5 a {
    text-decoration: none !important;
    color: #17A2B8;
    font-weight: bold;
  }

  a:hover {
    color: #0a7383;
  }
</style>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="https://t.me/username1223334">Nebula</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            User
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="/login_user">Sign In</a>
            <a class="dropdown-item" href="/add_user">Sign Up</a>
            <div class="dropdown-divider"></div>
            <% if(loggedIn) { %>
              <a class="dropdown-item" href="/logout">Sign Out</a>              
            <% } %>

          </div>
        </li>
      </ul>
      <div class="mr-5 text-info">
        <% if(loggedIn) { %>
          <i class="fa fa-user"></i> User: <a href="<%= `/user/${currentUser._id}` %>"><%= currentUser.name %></a>
        <% } else { %>
          <a href="/login_user">Log In</a>
        <% } %>
      </div>
    </div>
  </nav>